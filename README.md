# How to run
open the project in vscode or any code editor.
Have NodeJS installed and run `npm install`.
Then after the installation finishes run `npm run dev`. NodeJS should automatically open the project in your browser.
